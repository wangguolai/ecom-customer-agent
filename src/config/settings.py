# -*- coding: utf-8 -*-
"""参数与阈值 —— 散落在各模块的「魔数」集中地

为什么集中：这些值原本写在使用它们的模块顶部（agent.py 的步数上限、hybrid_retriever 的
候选集大小、cache 的锁超时…）。调参时得先想起「它定义在哪个文件」，且横向对比
（比如「所有和 token 有关的预算」）根本做不到。集中之后调参只动这一个文件。

⚠️ 这是 **Python 模块不是运行时配置**：改它要重新部署、要过 code review。刻意不用 .env/YAML——
安全与环境相关的（密钥、DB 地址、CS_ONLINE）已经在 .env；这里是**算法参数**，
它们和代码逻辑强耦合（改了要重跑评测），不该支持"改了不用回归"。

分组：Agent 循环 / 上下文与压缩 / 检索 / 后端可靠性 / 认证 / 会话
"""

# ═══════════════════════════════════════════════════════════════
# Agent 循环（ReAct）
# ═══════════════════════════════════════════════════════════════
MAX_STEPS = 8                    # ReAct 最大步数上限（防死循环）
MAX_TOOL_CALLS_PER_TURN = 5      # 单次 LLM 决策最多返回的工具调用数，超了直接让用户收敛（防一次狂调一堆工具）
MAX_TOOL_RESULT_LEN = 500        # 单个工具结果的字符截断上限（防 Token 爆炸）
MAX_OUTPUT_TOKENS = 2048         # 面向用户的 LLM 生成输出上限（纯兜底：防失控/封顶成本，不是「长度控制器」，简洁靠 prompt 约束）

# ═══════════════════════════════════════════════════════════════
# 上下文与压缩
# ═══════════════════════════════════════════════════════════════
MAX_HISTORY_TOKENS = 2000        # 历史 token 预算（演示用小值；这是「预算」不是「轮次」）
SUMMARY_MAX_TOKENS = 300         # 摘要输出 token 上限，防摘要比原文还大（负收益）

# ═══════════════════════════════════════════════════════════════
# 检索（混合检索参数改前务必重跑 eval_retrieval.py——它们和 Recall 强耦合）
# ═══════════════════════════════════════════════════════════════
DEFAULT_TOP_K = 3                # 检索返回条数
RRF_K = 60                       # RRF 融合的 k（论文默认值；实测 k 被 rerank 抹平，不值得调）
RERANK_CANDIDATE_N = 10          # RRF 融合后取多少候选交给 Rerank 精排。
# 新库（162 真实商品）实测 Recall@3：N=5→0.6194、N=10→0.6639、N=15/20/30/50→0.6583。
# 选 N=10 是新库上的最优（15 以上反而略降），且省一半 rerank 推理。
# ⚠️ 旧库（41 假商品）的最优是 N=15/20（0.9667）——数据换了参数要重标，别照搬旧结论。
VEC_SCORE_LOW = 0.4              # 召回层双低拒答：向量 top1 分数 < 此值且 BM25 无召回 → 判超知识库，拒答。
# ⚠️ 这是**召回层**阈值，不是 rerank 层。rerank 绝对阈值（0.6）已于 2026-08-20 被推翻——
# 口语 query 的 rerank 分数整体贴 0.5，绝对阈值会误杀（12 条口语 query 全空返回）。
RAG_TOP_K = 3                    # rag_pipeline 独立入口的检索条数

# ═══════════════════════════════════════════════════════════════
# 工具 HTTP 调用
# ═══════════════════════════════════════════════════════════════
REQUEST_TIMEOUT = 2.0            # 秒。工具调后端的超时（短超时 + 熔断 + 降级，不等重试）

# ═══════════════════════════════════════════════════════════════
# 后端可靠性（缓存 / 防击穿锁）
# ═══════════════════════════════════════════════════════════════
CACHE_EMPTY = "__EMPTY__"        # 空值哨兵（防穿透：命中它表示「真源确认不存在」）
CACHE_JITTER_SECONDS = 5         # TTL 抖动（防雪崩）
LOCK_PREFIX = "ecom:lock:"
LOCK_TTL = 5                     # 重建锁超时：持锁线程崩溃，锁最多卡 5s 自动释放，不永久阻塞
LOCK_WAIT = 0.05                 # 没抢到锁的线程重试读缓存间隔
LOCK_RETRY = 40                  # 最多重试 40 次（约 2s），仍 miss 则回源兜底（正确性优先）

# ═══════════════════════════════════════════════════════════════
# ⚠️ 认证（安全关键：改这里必须过 code review）
# ═══════════════════════════════════════════════════════════════
JWT_ALG = "HS256"
JWT_TOKEN_TTL = 3600             # 秒。短 TTL 是 JWT「无法主动失效」的主要缓解手段之一
PBKDF2_ROUNDS = 200_000
DEV_SECRET = "dev-only-secret-change-me"   # 本地 demo 默认密钥；生产必须用环境变量注入真密钥

# ═══════════════════════════════════════════════════════════════
# 会话层（Web 链路跨轮上下文）
# ═══════════════════════════════════════════════════════════════
SESSION_TTL = 1800               # 30 分钟滑动过期：每轮访问续期
SESSION_PREFIX = "ecom:session:"
SESSION_MAX_TURNS = 10           # 最多保留最近 N 轮（2N 条消息），防历史无限增长

# ═══════════════════════════════════════════════════════════════
# 用户记忆（长期记忆 / 画像沉淀）
# ═══════════════════════════════════════════════════════════════
MEMORY_TOP_K = 3                 # 检索返回条数（按 query 语义匹配，不是全量注入）
MEMORY_MIN_CONFIDENCE = 0.4      # 低于此置信度的记忆不注入（低置信度起步，注入时再筛一道）
MEMORY_MAX_CHARS = 300           # 单轮注入的画像总字符上限（超出按 score 序截断）
MEMORY_MAX_TOKENS = 200          # 单轮注入的 token 上限（兜底：字符估不准时的第二道闸）
MEMORY_MAX_PER_CATEGORY = 5      # 最小缓冲：同 (user_id, category) 只保留最新 N 条 active，
                                 # 超出的旧条置 status='superseded'。防「错误记忆永久固化」
                                 # （用户改口「我现在不养金毛了」时，旧条会被挤出而非永久驻留）
MEMORY_EXTRACT_MAX_TOKENS = 400  # 抽取输出上限（JSON 数组，几条事实足够）

# ═══════════════════════════════════════════════════════════════
# MQ（Redis List 模拟，退款工单异步化）
# ═══════════════════════════════════════════════════════════════
# 键用独立前缀 mq:，不放 ecom:——cache.flush() 只清 ecom:*（业务缓存），
# 而 MQ 队列和幂等标记是「在途数据」不是「缓存」：被 flush 清掉会导致在途消息静默丢失、
# 已受理的退款工单永不到账。独立前缀避开 flush 的 scan_iter("ecom:*")。
MQ_QUEUE_KEY = "mq:refund_queue"
MQ_DONE_PREFIX = "mq:refund:done:"
MQ_DONE_TTL = 604800             # 幂等标记 7 天：控制 key 增长（脱离 ecom: 后无 flush 兜底清理），过期由业务级唯一约束兜底
