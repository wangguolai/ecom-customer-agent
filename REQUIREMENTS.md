# 电商客服需求清单

> 从真实电商客服拆解（参考搜索结果 + 之前的 customer-service-agent-plan），需求是「调研出来的」，不是编的。

## 请求类型

电商客服处理的三层结构：**信息查询（RAG 解决 80%）→ 流程引导（工具调用）→ 异常处理（转人工）**。

### 1. 商品咨询（RAG）
- 「这个商品适合我吗」
- 「材质 / 规格是什么」
- 「有没有优惠活动」
- **链路**：意图识别 → 检索知识库 → 生成答案（带引用溯源）

### 2. 订单查询（工具调用）
- 「我的订单到哪了」
- 「订单什么时候发货」
- **链路**：`search_orders(订单号)` → 订单状态

### 3. 物流追踪（工具调用）
- 「物流到哪了」
- **链路**：`search_logistics(订单号)` → 物流轨迹

### 4. 库存查询（工具调用）
- 「XX 商品有货吗」
- **链路**：`check_stock(product_id)` → 库存（product_id 从 search_products 检索结果获取）

### 5. 售后 / 退货政策（RAG + 工具）
- 「能退吗」「退货流程是什么」
- **链路**：RAG 检索退换货政策 + `get_return_policy()`

### 6. 退款申请（工具 + 流程引导，写操作）
- 「我要退款」
- **链路**：多轮确认（订单号、原因）→ 退款工具
- ⚠️ 写操作需权限开关，默认只读

### 7. 转人工（兜底）
- 超出能力范围 → 记录工单
- **链路**：`transfer_to_human(问题描述)` → 工单

### 8. 多轮对话（记忆）
- 「它多少钱？」「适合我家 2 岁金毛吗」—— 记住上一句问的是哪款
- **链路**：短期记忆 + 上下文管理（截断/污染防护）

## 工具清单（7 个）

| 工具 | 参数 | 返回 | 读写 |
|------|------|------|------|
| `search_products` | query | 商品知识库检索 | 只读 |
| `search_orders` | 订单号 | 订单状态 | 只读 |
| `search_logistics` | 订单号 | 物流轨迹 | 只读 |
| `check_stock` | product_id | 库存/价格 | 只读 |
| `get_return_policy` | — | 退换货政策 | 只读 |
| `refund_order` | 订单号 | 退款工单（全额退款，待人工审批） | 写 |
| `transfer_to_human` | 问题描述 | 工单 | 写 |

## 安全边界

- 工具默认只读，写操作（退款/改单）需显式权限开关
- 工具参数校验（订单号格式、product_id 存在性）
- Prompt Injection 防御：外部数据放数据区，指令与数据分离
