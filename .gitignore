# Python
__pycache__/
*.pyc
*.pyo

# Qdrant 本地向量库（生成产物，可随时重建）
data/qdrant/

# SQLite 后端数据库（运行时生成，可随时重建）
data/ecommerce.db

# 环境变量
.env

# 评测临时结果（baseline 定稿保留，其余临时产物不入库）
tests/eval_results/answer_quality_after.json
tests/eval_results/answer_quality_retest.json

# IDE
.vscode/

# 本地临时产物（清理 C 盘脚本 + Redis 快照，非项目内容）
.tmp/
dump.rdb

# 本地-only（开源清理时从 git 历史移除，本地保留、不公开）：
# （要点清单/问答）+ Claude Code 项目级规则/agent + 笔记（教学/深挖）
CLAUDE.md
.claude/
references/
docs//
docs/deep-dive-points.md
docs/asset-accumulation-plan.md
